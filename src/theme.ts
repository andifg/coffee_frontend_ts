import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#374151",
      light: "#7b89a0",
    },
    secondary: {
      light: "#EED9CC",
      main: "#f44336",
    },
    warning: {
      main: "#d74d4d",
    },
  },
});

export default theme;
